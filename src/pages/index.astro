---
// Importamos la constante defaultLang desde el archivo de configuración de i18n
import { defaultLang } from '../i18n/ui';

// Redireccionamos a la página del idioma predeterminado
return Astro.redirect(`/${defaultLang}/`);
---
